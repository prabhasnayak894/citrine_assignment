'use strict';

/**
 * @ngdoc function
 * @name codeApp.controller:ViewTimesheetsCtrl
 * @description
 * # ViewTimesheetsCtrl
 * Controller of the codeApp
 */
angular.module('codeApp')
  .controller('ViewTimesheetsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
